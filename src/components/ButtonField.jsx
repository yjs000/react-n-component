
const ButtonField = ({children}) => {
    return (
        <div >
            <h1>버튼부</h1>
            <div>
                {children}
            </div>
        </div>

    );
};

export default ButtonField;
