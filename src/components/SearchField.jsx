
const SearchField = ({children}) => {
    return (
        <div >
            <h1>조회부</h1>
            <div>
                {children}
            </div>
        </div>

    );
};

export default SearchField;
